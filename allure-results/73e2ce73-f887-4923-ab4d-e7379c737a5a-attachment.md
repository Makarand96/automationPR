# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ApiLogin.spec.js >> Login
- Location: tests\ApiLogin.spec.js:20:5

# Error details

```
Error: locator.waitFor: Target page, context or browser has been closed
Call log:
  - waiting for locator('.card-body b').last() to be visible

```

# Test source

```ts
  1  | import {test,expect,request} from '@playwright/test';
  2  | const data = JSON.parse(JSON.stringify(require("../Utils_ts/placeorderTestData.json")));
  3  | const loginPayLoad = {userEmail:"mak7796@yopmail.com",userPassword:"Satara@12345"};
  4  | 
  5  | let token;
  6  | test.beforeAll(async()=>
  7  | {
  8  | const apiContext = await request.newContext();
  9  | const loginResponse = await apiContext.post("https://rahulshettyacademy.com/api/ecom/auth/login",
  10 |     {
  11 |     data:loginPayLoad,
  12 |     })
  13 |     const responseBody = await loginResponse.json();
  14 |     expect(loginResponse.ok()).toBeTruthy();
  15 |     token = responseBody.token;
  16 |     console.log("Token:", token);
  17 |     
  18 | });
  19 | 
  20 | test('Login', async ({ page }) => {
  21 | //    await page.goto('https://rahulshettyacademy.com/client/#/auth/login');
  22 | //    await page.getByPlaceholder('email@example.com').fill('mak7796@yopmail.com');
  23 | //    await page.getByPlaceholder('enter your passsword').fill('Satara@12345');
  24 | //    await page.locator('#login').click();
  25 | 
  26 | await page.addInitScript((value)=>{window.localStorage.setItem('token',value);},token);
  27 |  //   await page.evaluate((value)=>{localStorage.setItem('token',value);},token);
  28 | 
> 29 |    await page.locator('.card-body b').last().waitFor();    // we are waiting here for specific element. Wait for works for only 1 element, so we provided first/last
     |                                              ^ Error: locator.waitFor: Target page, context or browser has been closed
  30 |    console.log(await page.locator('.card-body b').allTextContents()); // we are using wait above as method allTextContents  do not support auto wait from  Playwright
  31 | 
  32 | })
  33 | 
  34 | 
```